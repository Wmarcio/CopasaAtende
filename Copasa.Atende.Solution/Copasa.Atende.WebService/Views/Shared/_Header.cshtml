<header class="main-header">
    <nav class="navbar navbar-static-top">
        <div class="container-fluid">

            <!-- Sigla/Logomarca do sistema-->
            <div class="navbar-header">
                @Html.ActionLink("Copasa Atende", "Index", "Home", new { area = "" }, new { @class = "navbar-brand text-bold" })
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
                    <i class="fa fa-bars"></i>
                </button>
            </div>

            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="navbar-collapse">
                <ul class="nav navbar-nav">
                    <li class="active">@Html.ActionLink("Início", "Index", "Home", new { area = "" }, null)</li>
                    <li>@Html.ActionLink("API", "Index", "Help", new { area = "" }, null)</li>
                </ul>

                <!-- Menus de informacoes a direita -->
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown user user-menu">
                        <a href="#" class="dropdown-toggle hidden-xs" data-toggle="dropdown">
                            <span class="fa fa-user"></span>
                            <span class="hidden-xs">NOME DO USUÁRIO</span>
                        </a>
                    </li>
                    <li>
                        <a href="@Url.Action("Logout","Account")" title="Sair">
                            <span class="glyphicon glyphicon-off"></span>
                        </a>
                    </li>
                </ul>

            </div>
            <!-- /.navbar-collapse -->

        </div>
        <!-- /.container-fluid -->
    </nav>
</header>

